# 05 - Task: sub-tab "สัญญาณที่ประชุม" (เขียนโค้ดจริง)

Type: task
Status: closed
Claimed: hermes/2026-08-10
Blocked by: 01, 04

## Question

ไม่มีอะไรให้ตัดสิน — สร้าง UI ตาม layout ที่ ticket 01 ขุดมา

## ⚠️ ticket นี้เขียนโค้ดจริง

หยุดให้ user ตรวจก่อน commit · รันเทสต์จริงรายงานเลขจริง · ติดขัดให้หยุดถาม

## สิ่งที่ต้องสร้าง

`frontend/src/components/tools/BoardroomSignalsDashboard.tsx` + ต่อเป็น sub-tab ใน `BondCrisisPage.tsx`

**ข้อจำกัดดีไซน์:** ไม่มี Tailwind — inline style + `INK` palette แบบเดียวกับ `SignalsDashboard.tsx` (ซึ่งเป็นหน้าที่ใกล้เคียงที่สุด ใช้เป็นแบบได้เลย แต่**ต้องดูต่างจากมันพอ**ให้ผู้ใช้ไม่สับสนว่าเป็นสัญญาณคนละแหล่ง)

ส่วนที่ต้องมี (copy ไทยเป๊ะๆ จากผลของ ticket 01):
1. **สองแท็บ**: กำลังนับถอยหลัง / สรุปแล้ว
2. **แถบสถิติ**: อัตราถูกทาง · P&L สดเฉลี่ย · P&L สด (ยังไม่ปิด) · P&L สรุปแล้ว + หมายเหตุ *"รวมแบบน้ำหนักเท่ากันทุกสัญญาณ"*
3. **แยกสองกลุ่ม**: กลุ่มราคา (%) — *"ETF · ดัชนี · ทอง/น้ำมัน · FX — P&L คิดเป็น % จากราคาเข้า"* และ กลุ่ม Yield / สเปรด (bp)
4. **การ์ดสัญญาณ**: สินทรัพย์ · ทิศทาง (ยีลด์/สเปรด สำหรับกลุ่ม bp) · ราคาเข้า → ราคาปัจจุบัน · P&L · นับถอยหลังจนครบกำหนด · **ผลจุดตรวจ +1/+3/+7** (ยังไม่ถึงเวลาแสดง *"ยังไม่ถึงเวลา"*) · ลิงก์กลับไปมติต้นทาง
5. **ความสดของราคา** — ตามที่ ticket 03 ตัดสิน (กลุ่ม bp อาจเก่าถึง 1 วัน ต้องบอกให้เห็น)
6. **empty state**: *"ยังไม่มีสัญญาณครบกำหนดตัดสิน — ผลจุดตรวจระหว่างทางดูได้บนการ์ดและสรุปด้านล่าง"* + *"คิวแรกครบกำหนด"*
7. **disclaimer**: *"มุมมอง (ไม่เข้าบัญชี)"* — ไม่ใช่คำแนะนำการลงทุน ไม่ใช่ออเดอร์จริง
8. **สถิติตอนข้อมูลน้อย** — แสดงข้อความว่ายังสะสมไม่พอ ไม่ใช่เปอร์เซ็นต์หลอกตา

## เทสต์ (`BoardroomSignalsDashboard.test.tsx`)
- render สองกลุ่มแยกกันถูกต้องจาก fixture
- สลับแท็บ นับถอยหลัง ↔ สรุปแล้ว
- จุดตรวจที่ยังไม่ถึงเวลาแสดง "ยังไม่ถึงเวลา"
- P&L สีถูก (บวก/ลบ/ศูนย์)
- empty state ทั้งสองแท็บ
- ราคาดึงไม่ได้ → "—" ไม่ใช่ 0 หรือค่าว่าง
- สถิติตอนข้อมูลน้อยไม่โชว์เปอร์เซ็นต์

## Answer

**สร้างเสร็จ + เทสต์ผ่านจริง (2026-08-10) — ยังไม่ commit รอ user ตรวจตามกติกาแผน**

### ไฟล์ที่สร้าง/แก้ (frontend)
| ไฟล์ | เนื้อหา |
|---|---|
| `frontend/src/components/tools/BoardroomSignalsDashboard.tsx` (ใหม่) | sub-tab สัญญาณจากที่ประชุม — แถบสถิติ 4 ช่อง · แท็บ นับถอยหลัง/สรุปแล้ว · กลุ่มราคา % (ฟ้า)/Yield-สเปรด bp (ม่วง) · การ์ดสัญญาณ (สินทรัพย์+ทิศทาง+P&L สี+นับถอยหลัง+จุดตรวจ d1/d3/d7+ราคาเข้า→ปัจจุบัน+DD+conf+ไปที่ประชุม) · มุมมอง (ไม่เข้าบัญชี) · สรุปจุดตรวจ · สถิติรายสินทรัพย์ · empty state · cold-start disclosure · ป้าย "ราคารายวัน" (bp — FRED) · disclaimer — polling 90s + นับถอยหลัง 30s |
| `frontend/src/components/tools/BoardroomSignalsDashboard.test.tsx` (ใหม่) | 7 เทสต์ (mock client — ทุกข้อบังคับตาม ticket) |
| `frontend/src/pages/BondCrisisPage.tsx` (แก้) | + sub-tab "สัญญาณที่ประชุม" · `onGoMeeting` → สลับแท็บ + focus มติ |
| `frontend/src/components/tools/BoardroomDashboard.tsx` (แก้) | + prop `focusMeetingId` (กด "ไปที่ประชุม" → โหลดมตินั้นอัตโนมัติ) |
| `frontend/src/api/types.ts` + `client.ts` (แก้) | `BoardroomStance/Stats/Payload` + `getBoardroomStances()` |

### เลขเทสต์จริง (รันสด 2026-08-10)
- `vitest run BoardroomSignalsDashboard.test.tsx` → **7 passed**
- `vitest run` (ทั้งชุด) → **603 passed / 73 files** (596 เดิม + 7 ใหม่)
- `npx tsc --noEmit` → **0 error**
- `pytest tests/` (backend — เช็ค engine hook ไม่พัง) → **506 passed**
- `hermes verify --json` → **ok: true**

### ครอบคลุมตาม ticket
1. ✅ สองแท็บ นับถอยหลัง / สรุปแล้ว (+สลับเทสต์)
2. ✅ แถบสถิติ: อัตราถูกทาง · P&L สดเฉลี่ย · P&L สรุปแล้ว (แยก pct/bp + "(เฉลี่ย · n)") + tooltip "รวมแบบน้ำหนักเท่ากันทุกสัญญาณ"
3. ✅ แยกสองกลุ่ม (ราคา % / Yield-สเปรด bp) พร้อมคำอธิบายไทยเป๊ะจาก ticket 01
4. ✅ การ์ดสัญญาณ: สินทรัพย์ · ทิศทาง (LONG↑/SHORT↓ + ยิลด์/สเปรด) · ราคาเข้า→ปัจจุบัน · P&L · นับถอยหลัง · จุดตรวจ +1/+3/+7 ("ยังไม่ถึงเวลา") · ลิงก์ "ไปที่ประชุม" → โฟกัสมติ
5. ✅ ความสด: กลุ่ม bp แสดงป้าย "ราคารายวัน" (FRED อาจเก่า 1 วัน) — ตาม ticket 03
6. ✅ empty state ทั้งสองแท็บ + "คิวแรกครบกำหนด"
7. ✅ disclaimer "มุมมอง (ไม่เข้าบัญชี) — ไม่ใช่คำแนะนำการลงทุน ไม่ใช่ออเดอร์จริง"
8. ✅ สถิติตอนข้อมูลน้อย: cold_start → "รอข้อมูลเพิ่ม" + "ยังสะสมสถิติไม่พอ — รออย่างน้อย 10 รายการ" (ไม่โชว์ % หลอกตา)
9. ✅ เพิ่ม: ราคาดึงไม่ได้ → "—" + ป้าย "ตรวจไม่ได้ — ไม่มีราคา" · มุมมอง (qualified=false) แยก section · สรุปจุดตรวจ d1/d3/d7 · สถิติรายสินทรัพย์ (W/L/winRate cold-start/P&L avg) · DD

### หมายเหตุ
- payload จาก backend 04: stances + stats (win_rate=None เมื่อ n<10 — frontend แสดง "รอข้อมูลเพิ่ม")
- quote_at ยังไม่มาจาก backend (กลุ่ม pct ป้ายความสดละเอียด — ตัดสินใจตัดออก v1 — ป้าย bp ครอบคลุมกรณีหลัก; เติมได้ทีหลัง)
- `ไปที่ประชุม` ใช้ state ใน BondCrisisPage + prop ใหม่ของ BoardroomDashboard (แก้ไฟล์เดิมนิดเดียว)

**ส่งต่อ:** ticket 06 (spec + close) ได้ layout/copy/ตัวเลขครบ

**⛔ ยังไม่ commit — รอ user ตรวจก่อน (กติกาข้อ 1 ของแผน)**
